<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mqitty">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

<!--        create send message activity-->
        <activity android:name=".ui.CreateSendActivity"/>

<!--        create receiver activity-->
        <activity android:name=".ui.CreateReceiveActivity"/>

<!--        modify send activity-->
        <activity android:name=".ui.SendModify"/>

<!--        modify receive activity-->
        <activity android:name=".ui.ReceiveModify"/>

<!--        chat receive activity-->
        <activity android:name=".ui.ChatActivity"/>
    </application>

</manifest>